/* eslint-disable react/prop-types */


const Card = ({ color }) => {
  return (
    <div className={`w-full h-64 ${color} m-2 rounded-md`}>
      {/* Card content goes here */}
    </div>
  );
};

export default Card;
